$if(CL_style)$
\documentclass[$CL_style$]{clv3}
$else$
\documentclass{clv3}
$endif$

\usepackage[letterpaper]{geometry}

\usepackage{multicol}

\usepackage{xcolor}
\definecolor{darkblue}{rgb}{0, 0, 0.5}

\usepackage{hyperref}
\hypersetup{colorlinks=true,citecolor=darkblue, linkcolor=darkblue, urlcolor=darkblue}

\usepackage[utf8]{inputenc}

\usepackage{natbib}
\bibliographystyle{compling}

$for(header-includes)$
$header-includes$
$endfor$

$if(issue)$
\issue{$issue.volume$}{$issue.number$}{$issue.year$}
$endif$

$if(history_dates)$
\historydates{$history_dates$}
$endif$

% Document head
$if(document_head)$
  \dochead{$document_head$}
$endif$

% Running title and running author name
$if(running_title)$
\runningtitle{$running_title$}
$endif$

$if(running_author)$
\runningauthor{$running_author$}
$endif$

% Title and author details
\begin{document}

$if(title)$
  \title{$title$}
$endif$

$for(author)$
  $if(author.contact_address)$
    \author{$author.name$\thanks{$author.contact_address$}}
  $else$
    \author{$author.name$\thanks{}}
  $endif$
  $if(author.affiliation)$
    \affil{$author.affiliation$}
  $endif$
$endfor$

\maketitle

$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

$body$

$if(bibliography)$
$if(natbib_citations)$
\begin{multicols}{2}

$if(acknowledgements)$
\section*{Acknowledgements}\label{acknowledgements}
\addcontentsline{toc}{section}{Acknowledgements}
$acknowledgements$
$endif$

\bibliography{$bibliography$}

\end{multicols}
$endif$
$else$
$if(acknowledgements)$
\section*{Acknowledgements}\label{acknowledgements}
\addcontentsline{toc}{section}{Acknowledgements}
$acknowledgements$
$endif$
$endif$

\end{document}
